% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_shape_and_factors.r
\name{link_shape_and_factors}
\alias{link_shape_and_factors}
\title{Link cell shape and gene expression via Canonical Correlation Analysis}
\usage{
link_shape_and_factors(
  obj,
  expr_mat,
  shape_mat,
  min_cells = 100,
  scale = TRUE,
  test_significance = FALSE,
  nperm = 1000L,
  perm_seed = NULL,
  verbose = FALSE,
  return_results = TRUE,
  output_dir = NULL
)
}
\arguments{
\item{obj}{A Seurat v5 object. Used only to validate that matrix row names
match known cell names; never mutated.}

\item{expr_mat}{Numeric matrix (cells x features). Expression-side input,
e.g. NMF factor scores. Row names must match Seurat cell names.}

\item{shape_mat}{Numeric matrix (cells x shape PCs). Shape-side input from
TPCA. Row names must match Seurat cell names.}

\item{min_cells}{Integer. If fewer cells remain after intersecting the three
cell sets, the function errors (default 100).}

\item{scale}{Logical. Whether to z-score \code{shape_mat} and
\code{expr_mat} before CCA (default TRUE).}

\item{test_significance}{Logical. Whether to compute deflation-based
permutation p-values via \code{cca_pvalues()} (default FALSE).}

\item{nperm}{Integer. Number of permutations for significance testing.}

\item{perm_seed}{Integer or NULL. RNG seed for permutation testing.}

\item{verbose}{Logical. Whether to emit progress messages (default FALSE).}

\item{return_results}{Logical. When TRUE (default) the result list is
returned. When FALSE, the result is serialized to
\code{<output_dir>/all.rds} and the file path is returned.}

\item{output_dir}{Character or NULL. Directory for serialized output when
\code{return_results = FALSE}. When NULL a temporary directory is used
and not cleaned up. Ignored when \code{return_results = TRUE}.}
}
\value{
When \code{return_results = TRUE}: a list containing
\code{CC_Corr_Coefs}, \code{Shape_Corr_With_CSP}, \code{Exp_Corr_With_CEP},
\code{CSP_Scores}, \code{CEP_Scores}, \code{CSP_Vectors},
\code{CEP_Vectors}, \code{Misc_CCA}, and \code{Anchor_Features}. If
\code{test_significance = TRUE}, also \code{P_Value_Info}.

When \code{return_results = FALSE}: a list with element
\code{output_path} (character scalar, path to the serialized RDS).
}
\description{
Runs CCA between a shape PC matrix and an expression factor matrix.
Operates on a single pre-filtered cell set. For multi-group workflows,
call this function once per group in an external loop, passing pre-subsetted
matrices each time.
}
\seealso{
\code{\link{store_kstitch_results}}, \code{\link{load_kstitch_results}}
}
