% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat_contour_export.R
\name{export_seurat_contours}
\alias{export_seurat_contours}
\title{Export contours from a Seurat object and run TPCA}
\usage{
export_seurat_contours(
  obj,
  seg_list,
  contour_type = c("cell", "nucleus"),
  output_dir,
  cell_ids = NULL,
  num_vertices = 50L,
  eta = 1,
  use_parallel = FALSE,
  num_threads = 8L,
  frechet_mean_tol = 1e-04,
  use_cache = TRUE,
  max_frechet_iter = 1000L
)
}
\arguments{
\item{obj}{A Seurat object. Used to optionally subset contours to
cells present in the object.}

\item{seg_list}{The list returned by \code{ReadXenium} or
\code{ReadNanostring}.}

\item{contour_type}{One of \code{"cell"} or \code{"nucleus"}.}

\item{output_dir}{Directory for TPCA intermediate outputs.}

\item{cell_ids}{Optional character vector of cell IDs to analyse.
Defaults to all cells in \code{obj}.}

\item{num_vertices}{Integer. Vertices to sample per contour (default 50).}

\item{eta}{Numeric. TPCA eta parameter (default 1).}

\item{use_parallel}{Logical. Use parallel processing (default FALSE).}

\item{num_threads}{Integer. Threads when \code{use_parallel = TRUE}.}

\item{frechet_mean_tol}{Numeric. Convergence tolerance for Frechet mean.}

\item{max_frechet_iter}{Integer. Maximum Frechet mean iterations.}
}
\value{
A list in the format expected by \code{store_tpca_results()}.
}
\description{
Accepts the raw segmentation list returned by \code{ReadXenium} or
\code{ReadNanostring}, normalises the contour data frame, passes it
directly to kendall_tpca (no intermediate parquet), runs TPCA, and returns a
result ready for \code{store_tpca_results()}.
}
