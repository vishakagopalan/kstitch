% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prioritize_cca_components.r
\name{prioritize_cca_components}
\alias{prioritize_cca_components}
\title{Prioritize CCA components by cross-loading stability}
\usage{
prioritize_cca_components(
  shape_mat,
  nmf_mat,
  ccs_to_consider,
  shape_side_rank = 5,
  factor_side_rank = 5,
  num_replicates = 100,
  subsample_frac = 0.9,
  cell_subsample = NULL,
  num_times_among_top_ranks = 75
)
}
\arguments{
\item{shape_mat}{Numeric matrix, cells x shape features, rownames = cell
names. Should be the same (optionally scaled) matrix passed to
\code{\link{link_shape_and_factors}}.}

\item{nmf_mat}{Numeric matrix, cells x NMF factors, rownames = cell names.}

\item{ccs_to_consider}{Integer vector of component indices to evaluate
(e.g. \code{1:3}).}

\item{shape_side_rank}{Integer. A shape feature must rank within the top
\code{shape_side_rank} positions to count toward its tally. Default 5.}

\item{factor_side_rank}{Integer. Same threshold for NMF factors. Default 5.}

\item{num_replicates}{Integer. Number of subsampling replicates. Default 100.}

\item{subsample_frac}{Numeric in (0, 1]. Fraction of cells to use per
replicate. Default 0.9.}

\item{cell_subsample}{Optional character vector of cell names to restrict
the pool before subsampling (e.g. already-intersected cells for a group).
Distinct from \code{group.by} filtering — this is purely for replicate
subsampling.}

\item{num_times_among_top_ranks}{Integer. Minimum number of replicates in
which a feature must appear within the top-rank threshold to be retained.
Default 75.}
}
\value{
A list with two data frames:
\describe{
\item{Shape}{Shape features passing the stability filter, with columns
\code{feature_name}, \code{CC}, \code{cross_loading_mag}, \code{n}.}
\item{Factor}{NMF factors passing the stability filter, same columns.}
}
}
\description{
Identifies the most stable features on each canonical component via
repeated subsampling. On each replicate, \code{\link{run_cca}} is run on a
random fraction of cells and features are ranked by their absolute
cross-loading magnitude. Features that consistently appear among the top
ranks across replicates are retained.
}
\seealso{
\code{\link{link_shape_and_factors}}, \code{\link{run_cca}}
}
